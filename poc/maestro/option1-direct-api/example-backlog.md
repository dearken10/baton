# Example .baton/backlog.md

Copy this to `<your-project-root>/.baton/backlog.md` to unlock
`initiate` candidates for that project. The parser reads only
`- [ ]` lines under the `## TODO` heading.

---

## TODO
- [ ] add a `tfa feedback` CLI that appends to ~/.baton/feedback.jsonl
- [ ] split intentSummarizer.ts into queue + render
- [ ] write integration test for hookForwarderSource pid recycling

## DOING
- [x] master-mind — running in worktree master-mind
