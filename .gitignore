.DS_Store
node_modules/
.env
.env.local
.env.*.local
dist/
build/
out/
*.log
.vscode/
.idea/
.code24/
.claude/*
# Track project-level skills (maestro-tick, etc.) — they're product code.
!.claude/skills/
.claude/skills/*/state/

# Electron app build + deps
app/node_modules/
app/out/
app/dist/
app/tsconfig.node.tsbuildinfo

# baton: per-project app state (worktrees, caches, …)
.baton/

# Local runtime scratch (Electron profiles, session DBs, mockups)
tmp/
